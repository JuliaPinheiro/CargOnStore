import { createTheme } from '@mui/material/styles';

 
const theme = createTheme({
  palette: {
    background: {
      default: '#f5f5f5',  
      paper: '#fafafa',  
    },
    primary: {
      main: '#212121',
    },
  },
 
});

export default theme;
